//Este archivo es para definir los modelos de datos que se van a usar en la base de datos, por ejemplo si se va a usar mongoose para mongodb o sequelize para sql
//Por ahora no se esta usando ningun modelo de datos, pero en un futuro se podria usar para definir los modelos de usuarios, productos, etc.

/* import mongoose from "mongoose";

const userSchema = new mongoose.Schema({
  name: String,
  email: String,
});

export default mongoose.model("User", userSchema); */
